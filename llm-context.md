# GoWebService

Update this project using the folling guidelines:

- Create website using Go lang
- Add css stylesheets using minimalist design
- Create paths /, /healthcheck, and /api
- / page should display 'Hello World'
- /healthcheck should provide 200 status code
- /api should return HTTP header info from orgin request
- Implement stub handlers
- Add proper HTTP status codes
- Set Content-Type to json
- Add error handling
- Add input validation
- Create response headers
- Add structured logging
- Seperate business logic
- Add tests
- Do not execute git commands

Update project to build a docker image using the following guidelines:

- Create dockerfile
- Using multistage to build project using go container
